using Microsoft.Json.Ling;
using System;
using System.IO;


namespace P2
{
    class Memoria
    {
        public void LeerMemoria()
        {
            string archivoDB = "../../;
            StreamReader reader = StreamReader(archivoDB);
            var dbJSON = reader.ReadToEnd();
            var 
        }
    }
}
